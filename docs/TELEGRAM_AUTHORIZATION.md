# Авторизация Telegram бота

## Описание

Система авторизации позволяет ограничить доступ к командам Telegram бота только для разрешенных пользователей. Это обеспечивает безопасность и предотвращает несанкционированное использование бота.

## Настройка

### 1. Включение/выключение авторизации

В файле `application.yml` или через переменные окружения:

```yaml
telegram:
  authorization:
    enabled: true  # Включить проверку авторизации (по умолчанию: true)
```

Или через переменную окружения:
```bash
TELEGRAM_AUTHORIZATION_ENABLED=true
```

### 2. Настройка разрешенных пользователей

Вы можете указать разрешенных пользователей двумя способами:

#### По User ID (рекомендуется)

User ID - это уникальный числовой идентификатор пользователя в Telegram. Это самый надежный способ, так как ID не меняется.

**Как узнать свой User ID:**
1. Напишите боту [@userinfobot](https://t.me/userinfobot) - он покажет ваш ID
2. Или используйте [@getidsbot](https://t.me/getidsbot)
3. Или посмотрите в логах бота при отправке команды (ID будет в логах)

**Настройка через application.yml:**
```yaml
telegram:
  authorization:
    allowed-user-ids: "123456789,987654321"  # Список ID через запятую
```

**Настройка через переменную окружения:**
```bash
TELEGRAM_ALLOWED_USER_IDS=123456789,987654321
```

#### По Username

Username - это имя пользователя в Telegram (например, @myusername). Менее надежно, так как username можно изменить.

**Настройка через application.yml:**
```yaml
telegram:
  authorization:
    allowed-usernames: "@myusername,@anotheruser"  # Список username через запятую
```

**Настройка через переменную окружения:**
```bash
TELEGRAM_ALLOWED_USERNAMES=@myusername,@anotheruser
```

**Примечание:** Символ `@` опционален - можно указать как `@myusername`, так и `myusername`.

### 3. Комбинированное использование

Можно использовать оба способа одновременно:

```yaml
telegram:
  authorization:
    enabled: true
    allowed-user-ids: "123456789"
    allowed-usernames: "@myusername"
```

Пользователь будет авторизован, если его ID **или** username есть в списке разрешенных.

## Примеры конфигурации

### Пример 1: Один пользователь по ID

```yaml
telegram:
  authorization:
    enabled: true
    allowed-user-ids: "123456789"
```

### Пример 2: Несколько пользователей по ID

```yaml
telegram:
  authorization:
    enabled: true
    allowed-user-ids: "123456789,987654321,555666777"
```

### Пример 3: Пользователь по username

```yaml
telegram:
  authorization:
    enabled: true
    allowed-usernames: "@myusername"
```

### Пример 4: Отключение авторизации (все пользователи имеют доступ)

```yaml
telegram:
  authorization:
    enabled: false
```

## Поведение при отсутствии доступа

Если пользователь, не указанный в списке разрешенных, попытается использовать команду бота, он получит сообщение:

```
🚫 Доступ запрещен

У вас нет прав на использование команд этого бота.
Обратитесь к администратору для получения доступа.
```

Команда не будет выполнена, и в логах будет записано предупреждение.

## Логирование

Система авторизации логирует следующие события:

- При старте приложения:
  - Статус авторизации (включена/выключена)
  - Количество разрешенных user ID
  - Количество разрешенных username
  - Предупреждение, если список разрешенных пользователей пуст

- При каждой команде:
  - Информация о пользователе (ID, username)
  - Результат проверки авторизации
  - Предупреждение при отказе в доступе

Пример логов:
```
🔒 [TelegramAuth] Authorization enabled
📋 [TelegramAuth] Loaded 1 allowed user IDs: [123456789]
📋 [TelegramAuth] Loaded 0 allowed usernames: []
✅ [TelegramAuth] User 123456789 authorized by user ID
❌ [TelegramAuth] Access denied for user ID: 999888777, username: N/A
```

## Безопасность

### Рекомендации

1. **Используйте User ID вместо username** - ID не меняется и более надежен
2. **Храните конфигурацию в переменных окружения** - не коммитьте ID пользователей в репозиторий
3. **Регулярно проверяйте логи** - следите за попытками несанкционированного доступа
4. **Используйте `.env` файл** - храните конфигурацию в `.env` файле, который не попадает в репозиторий

### Пример .env файла

```bash
# Telegram Authorization
TELEGRAM_AUTHORIZATION_ENABLED=true
TELEGRAM_ALLOWED_USER_IDS=123456789
# TELEGRAM_ALLOWED_USERNAMES=@myusername  # Опционально
```

## Отладка

Если авторизация не работает:

1. **Проверьте логи при старте** - убедитесь, что авторизация включена и пользователи загружены
2. **Проверьте формат ID** - должны быть числа без пробелов
3. **Проверьте формат username** - должны быть без пробелов, символ @ опционален
4. **Проверьте переменные окружения** - убедитесь, что они правильно загружены
5. **Проверьте логи при команде** - посмотрите, что пишется в логах при попытке выполнить команду

## Отключение авторизации для разработки

Для локальной разработки можно временно отключить авторизацию:

```yaml
telegram:
  authorization:
    enabled: false
```

**⚠️ ВНИМАНИЕ:** Не отключайте авторизацию в production окружении!






