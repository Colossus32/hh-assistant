# Предложения по улучшению производительности

Документ содержит анализ узких мест в flow получения, обработки и сохранения вакансий, а также рекомендации по оптимизации.

**Дата создания:** 2024  
**Статус:** Предложения для обсуждения и реализации

---

## 🔴 Критичные проблемы (высокий приоритет)

### 1. Отсутствие batch-операций для сохранения вакансий

**Проблема:**
- В `VacancyFetchService.kt:128` используется `map { vacancyRepository.save(it) }` - это создает N отдельных INSERT-запросов
- Отсутствует конфигурация Hibernate batch в `application.yml`

**Текущий код:**
```kotlin
val savedVacancies = allNewVacancies.map { vacancyRepository.save(it) }
```

**Рекомендация:**
```kotlin
val savedVacancies = vacancyRepository.saveAll(allNewVacancies)
```

**Конфигурация для `application.yml`:**
```yaml
spring:
  jpa:
    properties:
      hibernate:
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
        jdbc:
          batch_versioned_data: true
```

**Ожидаемый эффект:** 10-50x ускорение сохранения

---

### 2. Неэффективная проверка дубликатов

**Проблема:**
- Загружаются ВСЕ ID вакансий в память (`findAllIds()`)
- При большом количестве вакансий это медленно и потребляет много памяти
- Кэш инвалидируется при каждом новом сохранении

**Текущий код:**
```kotlin
val existingVacancyIds = vacancyIdsCache.get("all", {
    vacancyRepository.findAllIds().toSet()
}) ?: emptySet()
```

**Рекомендация:**
- Использовать `EXISTS` запросы для проверки конкретных ID
- Или batch-проверку через `WHERE id IN (...)`
- Или Bloom filter для предварительной фильтрации

**Ожидаемый эффект:** Снижение потребления памяти и ускорение проверки

---

### 3. Отсутствие транзакций для batch-операций

**Проблема:**
- Сохранение вакансий может происходить без явной транзакции
- Отдельные транзакции для каждой вакансии

**Рекомендация:**
```kotlin
@Transactional
suspend fun saveVacancies(vacancies: List<Vacancy>) {
    vacancyRepository.saveAll(vacancies)
}
```

---

## 🟡 Средний приоритет

### 4. Множественные фильтрации в памяти

**Проблема:**
Создаются промежуточные коллекции при фильтрации:
```kotlin
val filteredDtos = vacancyDtos.filter { ... }  // Фильтр по опыту
val validatedDtos = filteredDtos.filter { ... }  // Фильтр по keywords
val newVacancies = validatedDtos.map { ... }
```

**Рекомендация:**
- Объединить фильтры в один проход
- Использовать `asSequence()` для ленивой обработки

---

### 5. Инвалидация кэша при каждом сохранении

**Проблема:**
При каждом сохранении инвалидируется весь кэш ID, что приводит к повторной загрузке всех ID.

**Рекомендация:**
- Инкрементальное обновление кэша (добавлять новые ID)
- Или использовать более умную стратегию инвалидации

---

### 6. Сохранение навыков по одной записи

**Проблема:**
В `SkillExtractionService.kt:114` используется `save()` для каждой связи вакансия-навык.

**Рекомендация:**
- Batch-сохранение через `saveAll()`

---

## 🟢 Низкий приоритет

### 7. Неоптимальная пагинация

**Проблема:**
Фиксированная задержка 100ms между страницами замедляет процесс.

**Рекомендация:**
- Адаптивная задержка на основе rate limit
- Параллельная загрузка страниц (с учетом лимитов API)

---

### 8. Неэффективная проверка существования

**Проблема:**
Для каждой вакансии выполняется проверка в Set, но загрузка всех ID дорогая.

**Рекомендация:**
- Использовать `findAllById()` для проверки конкретных ID
- Или `EXISTS` запросы

---

## 📊 Ожидаемый общий эффект

- **Скорость сохранения:** 10-50x ускорение при batch-операциях
- **Память:** Снижение потребления при оптимизации проверки дубликатов
- **Нагрузка на БД:** Снижение количества запросов в 10-100 раз

---

## 📝 Примечания

- Все предложения требуют тестирования
- Рекомендуется внедрять постепенно, начиная с критичных проблем
- Необходимо мониторить производительность после каждого изменения

