# Событийная архитектура: Логирование и тестирование

## Логирование событий

Добавлен `EventLoggingAspect` для автоматического логирования всех событий Spring Event Bus:

- **📤 PUBLISHED**: Логирует публикацию событий с информацией о публикаторе и содержимом события
- **📥 RECEIVED**: Логирует получение событий слушателями с информацией о методе-обработчике
- **✅ PROCESSED**: Логирует успешное завершение обработки события

### Примеры логов:

```
📤 [EventBus] PUBLISHED: VacancyFetchedEvent by VacancyFetchService | vacancies: 5, keywords: 'Java'
📥 [EventBus] RECEIVED: VacancyFetchedEvent by VacancySchedulerService.handleVacancyFetched()
✅ [EventBus] PROCESSED: VacancyFetchedEvent by VacancySchedulerService.handleVacancyFetched()
```

## Валидация событийной архитектуры

### Поток событий:

1. **VacancyFetchService** → публикует `VacancyFetchedEvent`
2. **VacancyAnalysisService** → публикует `VacancyAnalyzedEvent` (для релевантных вакансий)
3. **CoverLetterQueueService** → слушает `VacancyAnalyzedEvent`, публикует:
   - `CoverLetterGeneratedEvent` (при успехе)
   - `CoverLetterGenerationFailedEvent` (при неудаче после всех попыток)
   - `VacancyReadyForTelegramEvent` (в обоих случаях)
4. **VacancyNotificationService** → слушает `VacancyReadyForTelegramEvent`, отправляет в Telegram
5. **VacancyStatusService** → публикует `VacancyStatusChangedEvent` при изменении статуса

### Преимущества для разделения на микросервисы

**✅ ДА, событийная архитектура значительно упростит разделение монолита на микросервисы:**

1. **Слабая связанность**: Сервисы общаются только через события, не знают друг о друге напрямую
2. **Независимое развертывание**: Каждый сервис можно вынести в отдельный микросервис без изменения логики
3. **Масштабируемость**: Каждый микросервис можно масштабировать независимо
4. **Технологическая независимость**: Микросервисы могут использовать разные технологии (Kotlin, Java, Python)
5. **Отказоустойчивость**: При падении одного микросервиса остальные продолжают работать

### Миграционный путь:

1. **Этап 1**: Заменить Spring Event Bus на message broker (RabbitMQ, Kafka, Redis Pub/Sub)
2. **Этап 2**: Вынести каждый сервис в отдельный микросервис
3. **Этап 3**: Добавить API Gateway для внешних запросов
4. **Этап 4**: Добавить service discovery (Consul, Eureka)

**Пример миграции:**
```kotlin
// Было (Spring Events):
eventPublisher.publishEvent(VacancyFetchedEvent(...))

// Станет (RabbitMQ):
rabbitTemplate.convertAndSend("vacancy.events", "fetched", event)
```

## Сложность тестирования

### Событийная архитектура делает тестирование **проще** в некоторых аспектах:

#### ✅ Преимущества:

1. **Изоляция**: Каждый сервис тестируется независимо
2. **Mock событий**: Легко мокировать события вместо сложных зависимостей
3. **Интеграционные тесты**: Можно тестировать полный flow через события
4. **Параллельное тестирование**: Тесты можно запускать параллельно

#### ⚠️ Сложности:

1. **Асинхронность**: События обрабатываются асинхронно, нужны специальные техники:
   - `CountDownLatch` для ожидания обработки
   - `@Async` требует настройки тестового контекста
   - Задержки в тестах (`delay()`, `runBlocking`)

2. **Порядок событий**: Нужно проверять порядок и количество событий

3. **Тестовый контекст**: Нужно настраивать Spring Test Context с `@EnableAsync`

### Примеры тестов:

#### Unit-тест с мокированием событий:
```kotlin
@Test
fun `should publish VacancyAnalyzedEvent`() {
    val eventPublisher = mockk<ApplicationEventPublisher>()
    val service = VacancyAnalysisService(..., eventPublisher)

    service.analyzeVacancy(vacancy)

    verify { eventPublisher.publishEvent(any<VacancyAnalyzedEvent>()) }
}
```

#### Интеграционный тест с реальными событиями:
```kotlin
@Test
fun `should process full event flow`() {
    val capturedEvents = mutableListOf<ApplicationEvent>()
    every { eventPublisher.publishEvent(any()) } answers {
        capturedEvents.add(firstArg())
    }

    // Действие
    fetchService.fetchAndSaveNewVacancies()

    // Проверка
    assertThat(capturedEvents).hasSize(1)
    assertThat(capturedEvents[0]).isInstanceOf(VacancyFetchedEvent::class.java)
}
```

## Покрытие тестами

### Текущее покрытие:

- ✅ **EventDrivenArchitectureTest**: Тесты событий и их flow
- ✅ **CoverLetterQueueServiceTest**: Тесты очереди генерации писем
- ✅ **VacancyNotificationServiceTest**: Тесты отправки в Telegram
- ✅ **VacancyStatusServiceTest**: Тесты изменения статусов
- ✅ **VacancyFetchServiceTest**: Тесты получения вакансий

### Цель: >80% покрытия

Для достижения покрытия >80% нужно:

1. **Добавить тесты для всех сервисов**:
   - `VacancyAnalysisService` (обновить существующие)
   - `VacancySchedulerService` (обновить существующие)
   - `CoverLetterGenerationService`
   - `VacancyContentValidator`
   - `ResumeService` (дополнить)
   - `PDFParserService` (дополнить)

2. **Добавить интеграционные тесты**:
   - Полный flow от получения вакансий до отправки в Telegram
   - Обработка ошибок и retry логики
   - Обработка событий в правильном порядке

3. **Использовать инструменты для измерения покрытия**:
   ```bash
   ./gradlew test jacocoTestReport
   ```

## Рекомендации

1. **Логирование**: Все события логируются автоматически через AOP
2. **Мониторинг**: Добавить метрики для событий (количество, время обработки)
3. **Трассировка**: Добавить correlation ID для отслеживания flow через события
4. **Тестирование**: Использовать `@SpringBootTest` для интеграционных тестов с реальным Event Bus


