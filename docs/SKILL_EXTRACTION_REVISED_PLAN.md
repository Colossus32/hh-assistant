# План реализации извлечения навыков из вакансий (пересмотренный)

## Цель
Извлекать навыки из сохраненных в БД вакансий (из описания и тегов, если есть), сохранять их в БД, привязанные к вакансиям, и предоставлять API + Telegram команду для получения списка самых востребованных навыков с частотой встречаемости.

## Анализ текущей ситуации

### ✅ Что уже есть:
1. **Вакансии сохраняются в БД** - `VacancyFetchService` сохраняет вакансии через `vacancyRepository.save()`
2. **Анализ вакансий через LLM** - `VacancyAnalysisService.analyzeVacancy()` анализирует вакансии
3. **Структура БД для навыков** - созданы таблицы `skills` и `vacancy_skills` (Этап 1)

### ✅ Что выяснено:
1. **В API HH.ru есть поле `key_skills`** - структурированный массив ключевых навыков
   - Навыки уже извлечены работодателем при создании вакансии
   - Это основной источник навыков (не нужно парсить описание для базовых случаев)
   - Формат: массив объектов с полем `name` (нужно проверить точную структуру)

2. **Когда извлекать навыки?**
   - Вариант А: При анализе вакансии через LLM (в `VacancyAnalysisService`)
   - Вариант Б: Отдельным процессом для всех вакансий в БД
   - **Рекомендация:** Вариант А - извлекать при анализе, так как анализ уже идет через LLM

3. **Из каких источников извлекать?**
   - Описание вакансии (`description`) - обязательно
   - Теги/навыки из API (`key_skills`, если есть) - если доступны
   - Название вакансии (`name`) - опционально
   - Требования (`snippet.requirement`) - опционально

## План реализации

### Этап 1: Расширение DTO для поддержки key_skills
**Цель:** Добавить поле `key_skills` в DTO для получения навыков из API

**Задачи:**
1. Добавить поле `key_skills` в `VacancyDto`
2. Создать DTO для элемента навыка (если нужно)
3. Обновить маппинг в `VacancyDtoExtensions.kt` для сохранения навыков

**Структура:**
```kotlin
data class KeySkillDto(
    val name: String,
    // возможно есть другие поля: id, etc.
)

data class VacancyDto(
    // ... существующие поля
    @JsonProperty("key_skills")
    val keySkills: List<KeySkillDto>? = null,
)
```

**Файлы:**
- `src/main/kotlin/com/hhassistant/client/hh/dto/VacancyDto.kt` - добавить поле `keySkills`
- `src/main/kotlin/com/hhassistant/client/hh/dto/KeySkillDto.kt` - создать DTO для навыка (если нужно)

---

### Этап 2: Извлечение навыков при анализе вакансии
**Цель:** Извлекать навыки из вакансии и сохранять их в БД при анализе

**Логика:**
1. В `VacancyAnalysisService.analyzeVacancy()` после успешного анализа:
   - **Приоритет 1:** Использовать `key_skills` из API (если есть)
   - **Приоритет 2:** Если `key_skills` пуст или недостаточно - извлечь навыки из описания через LLM
   - Нормализовать и сохранить навыки в БД
   - Создать связи `VacancySkill` для вакансии

2. **Стратегия извлечения:**
   - Если `key_skills` есть и содержит >= 3 навыка - использовать их
   - Если `key_skills` есть, но < 3 навыков - дополнить через LLM из описания
   - Если `key_skills` нет - извлечь через LLM из описания

3. **Извлечение через LLM (fallback):**
   - Использовать отдельный промпт для извлечения навыков
   - Формат ответа: JSON с массивом навыков
   - Минимум 3 навыка, максимум 20

3. **Нормализация:**
   - Привести к lowercase
   - Удалить лишние пробелы
   - Использовать словарь синонимов (если есть)

4. **Сохранение:**
   - Для каждого навыка: найти или создать `Skill`
   - Увеличить `occurrenceCount` в `Skill`
   - Создать `VacancySkill` для связи

**Файлы для создания:**
- `src/main/kotlin/com/hhassistant/service/SkillExtractionService.kt` - сервис извлечения навыков
- `src/main/resources/prompts/skill-extraction-prompt.txt` - промпт для LLM

**Файлы для изменения:**
- `src/main/kotlin/com/hhassistant/service/VacancyAnalysisService.kt` - добавить вызов извлечения навыков
- `src/main/kotlin/com/hhassistant/config/PromptConfig.kt` - добавить промпт для извлечения навыков

---

### Этап 3: Сервис статистики навыков
**Цель:** Предоставить методы для получения статистики навыков

**Функционал:**
1. Получить топ навыков по частоте встречаемости
2. Рассчитать процент встречаемости (сколько % вакансий содержат навык)
3. Подсчитать общее количество проанализированных вакансий

**Файлы для создания:**
- `src/main/kotlin/com/hhassistant/service/SkillStatisticsService.kt`

**Методы:**
- `getTopSkills(limit: Int): List<SkillStatistics>` - топ навыков
- `getSkillFrequency(skillName: String): Double?` - частота конкретного навыка
- `getTotalAnalyzedVacancies(): Long` - общее количество проанализированных вакансий

**DTO:**
```kotlin
data class SkillStatistics(
    val skillName: String,
    val occurrenceCount: Int,
    val totalVacanciesAnalyzed: Int,
    val frequencyPercentage: Double,
)
```

---

### Этап 4: REST API для статистики навыков
**Цель:** Предоставить HTTP endpoint для получения статистики

**Endpoints:**
- `GET /api/skills/top?limit=20` - топ навыков по популярности
  - Response: `{ "skills": [...], "totalVacanciesAnalyzed": 330 }`

**Файлы для создания:**
- `src/main/kotlin/com/hhassistant/web/SkillStatisticsController.kt`

**Response формат:**
```json
{
  "skills": [
    {
      "skillName": "Kotlin",
      "occurrenceCount": 150,
      "frequencyPercentage": 45.5,
      "totalVacanciesAnalyzed": 330
    },
    {
      "skillName": "Spring Boot",
      "occurrenceCount": 120,
      "frequencyPercentage": 36.4,
      "totalVacanciesAnalyzed": 330
    }
  ],
  "totalVacanciesAnalyzed": 330
}
```

---

### Этап 5: Telegram команда для получения статистики
**Цель:** Добавить команду `/skills` в Telegram бота

**Функционал:**
- Команда `/skills` - получить топ-20 навыков
- Команда `/skills 10` - получить топ-N навыков
- Форматированный ответ в Telegram

**Файлы для изменения:**
- `src/main/kotlin/com/hhassistant/service/TelegramCommandHandler.kt` - добавить обработку команды `/skills`

**Логика:**
1. Вызвать REST API `/api/skills/top?limit={limit}`
2. Отформатировать ответ для Telegram
3. Отправить сообщение пользователю

**Пример ответа в Telegram:**
```
📊 Топ навыков по популярности:

1. Kotlin - 45.5% (150 вакансий)
2. Spring Boot - 36.4% (120 вакансий)
3. PostgreSQL - 28.8% (95 вакансий)
...

Всего проанализировано: 330 вакансий
```

---

## Вопросы для уточнения

### 1. Источник навыков
**Вопрос:** Есть ли в API HH.ru поле `key_skills` или `skills` в ответе вакансии?
**Действие:** Нужно проверить реальный ответ API или документацию

### 2. Когда извлекать навыки
**Вопрос:** Извлекать навыки только при анализе вакансии через LLM, или также для всех вакансий в БД (включая те, что уже проанализированы)?
**Рекомендация:** 
- Для новых вакансий - при анализе
- Для существующих - можно сделать отдельную команду/задачу для переизвлечения

### 3. Обработка существующих вакансий
**Вопрос:** Нужно ли извлекать навыки для вакансий, которые уже есть в БД, но были проанализированы до добавления функционала извлечения навыков?
**Варианты:**
- А) Извлекать навыки только для новых анализов
- Б) Создать задачу для переизвлечения навыков из всех вакансий в БД
- В) Извлекать навыки при следующем анализе (если вакансия переанализируется)

### 4. Нормализация навыков
**Вопрос:** Нужен ли словарь синонимов для нормализации навыков (например, "Kotlin" = "Kotlin Developer" = "Kotlin программист")?
**Рекомендация:** Начать без словаря, добавить позже при необходимости

### 5. Минимальное количество вакансий
**Вопрос:** С какого количества встречаемости навык считается "востребованным" и должен попадать в топ?
**Рекомендация:** Показывать все навыки, сортировать по частоте. Можно добавить фильтр позже.

---

## Последовательность реализации

1. **Этап 1** - Проверить API и расширить DTO (если нужно)
2. **Этап 2** - Извлечение навыков при анализе
3. **Этап 3** - Сервис статистики
4. **Этап 4** - REST API
5. **Этап 5** - Telegram команда

## Оценка времени
- Этап 1: 30 минут (проверка API)
- Этап 2: 2-3 часа (извлечение навыков)
- Этап 3: 1 час (сервис статистики)
- Этап 4: 1 час (REST API)
- Этап 5: 1 час (Telegram команда)

**Итого:** ~6-7 часов

---

## Следующие шаги

1. ✅ **Выяснено:** В API есть `key_skills` - используем как основной источник
2. **Начать реализацию** с Этапа 1: Расширение DTO
3. **Этап 2:** Извлечение навыков (приоритет: `key_skills` → LLM fallback)
4. **Этапы 3-5:** Статистика, API, Telegram команда

## Обновленная стратегия

**Гибридный подход:**
1. **Основной источник:** `key_skills` из API (быстро, точно, бесплатно)
2. **Дополнительный источник:** LLM извлечение из `description` (только если `key_skills` недостаточно)
3. **Нормализация:** Привести все навыки к единому виду
4. **Сохранение:** В БД с привязкой к вакансии

**Преимущества:**
- Минимизация использования LLM (экономия ресурсов)
- Использование структурированных данных от работодателя (более точно)
- Fallback на LLM для вакансий без `key_skills`

