# Инструкция по разворачиванию на Windows

Подробная инструкция для запуска HH Assistant на Windows.

## 📋 Предварительные требования

### Обязательные компоненты:

1. **Docker Desktop для Windows**
   - Скачайте с https://www.docker.com/products/docker-desktop/
   - Установите и запустите Docker Desktop
   - Убедитесь, что WSL2 включен (Docker Desktop автоматически настроит)

2. **JDK 17 или выше**
   - Скачайте с https://adoptium.net/ или используйте OpenJDK
   - Установите и добавьте в PATH
   - Проверьте: `java -version`

3. **Git**
   - Скачайте с https://git-scm.com/download/win
   - Установите с настройками по умолчанию

4. **Ollama** (для локальной LLM)
   - Скачайте с https://ollama.ai/download
   - Установите Ollama
   - Подробнее: см. [OLLAMA_SETUP.md](OLLAMA_SETUP.md)

### Опциональные компоненты:

- **Postman** или **curl** - для тестирования API
- **DBeaver** или **pgAdmin** - для работы с PostgreSQL (опционально)

---

## 🚀 Варианты запуска

### Вариант 1: Полный запуск с HH.ru API (рекомендуется)

Этот вариант использует реальный HH.ru API для поиска вакансий.

#### Шаг 1: Получение токена HH.ru

1. Зарегистрируйтесь на https://hh.ru
2. Перейдите в настройки: https://hh.ru/account/applications
3. Создайте новое приложение (Personal Access Token)
4. Скопируйте токен доступа

#### Шаг 2: Настройка Telegram бота (опционально)

Если хотите получать уведомления в Telegram:

1. Найдите бота @BotFather в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен бота
4. Отправьте `/start` вашему боту
5. Найдите ваш Chat ID:
   - Отправьте сообщение боту
   - Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - Найдите `"chat":{"id":123456789}` - это ваш Chat ID

#### Шаг 3: Установка и запуск Ollama

1. Скачайте Ollama с https://ollama.ai/download
2. Установите (Ollama запустится автоматически как сервис)
3. Откройте PowerShell и выполните:
   ```powershell
   ollama pull qwen2.5:7b
   ```
   Это загрузит модель (~4GB). Процесс может занять время.

4. Проверьте работу Ollama:
   ```powershell
   curl http://localhost:11434/api/tags
   ```
   Должен вернуться JSON со списком моделей.

#### Шаг 4: Настройка переменных окружения

1. Создайте файл `.env` в корне проекта:
   ```powershell
   # В PowerShell
   New-Item -Path .env -ItemType File
   ```

2. Откройте `.env` и добавьте:
   ```env
   # HH.ru API
   HH_ACCESS_TOKEN=your_hh_token_here
   
   # Telegram (опционально, можно оставить пустым)
   TELEGRAM_BOT_TOKEN=your_telegram_bot_token
   TELEGRAM_CHAT_ID=your_telegram_chat_id
   TELEGRAM_ENABLED=true
   
   # Ollama
   OLLAMA_BASE_URL=http://localhost:11434
   OLLAMA_MODEL=qwen2.5:7b
   
   # Database (значения по умолчанию)
   SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/hh_assistant
   SPRING_DATASOURCE_USERNAME=hh_user
   SPRING_DATASOURCE_PASSWORD=hh_pass
   ```

#### Шаг 5: Запуск PostgreSQL

1. Откройте PowerShell в корне проекта
2. Запустите PostgreSQL:
   ```powershell
   docker-compose up -d postgres
   ```

3. Проверьте, что контейнер запущен:
   ```powershell
   docker ps
   ```
   Должен быть виден контейнер `hh-postgres`

4. Подождите 10-15 секунд для инициализации БД

#### Шаг 6: Подготовка резюме (опционально)

Если у вас есть PDF резюме:

1. Создайте папку `resumes` в корне проекта (если её нет)
2. Поместите ваш PDF файл резюме в эту папку
3. В `application.yml` укажите путь (или оставьте по умолчанию)

#### Шаг 7: Настройка конфигурации поиска

Приложение требует активную конфигурацию поиска в БД. После первого запуска:

1. Подключитесь к PostgreSQL:
   ```powershell
   docker exec -it hh-postgres psql -U hh_user -d hh_assistant
   ```

2. Создайте конфигурацию поиска:
   ```sql
   INSERT INTO search_configs (keywords, min_salary, area, experience, is_active)
   VALUES ('Kotlin Developer', 150000, 'Москва', 'От 3 лет', true);
   ```

   Или используйте другой запрос, например:
   ```sql
   INSERT INTO search_configs (keywords, min_salary, area, experience, is_active)
   VALUES ('Java Developer', 200000, 'Санкт-Петербург', 'От 5 лет', true);
   ```

3. Выйдите: `\q`

#### Шаг 8: Запуск приложения

1. В PowerShell выполните:
   ```powershell
   ./gradlew bootRun
   ```

2. Дождитесь сообщения:
   ```
   Started Application in X.XXX seconds
   ```

3. Приложение будет:
   - Проверять новые вакансии каждые 15 минут
   - Анализировать их через Ollama
   - Отправлять релевантные в Telegram (если настроено)

#### Шаг 9: Проверка работоспособности

1. **Health Checks** (в браузере или через curl):
   ```
   http://localhost:8080/actuator/health
   ```
   
   Должен вернуться JSON со статусами:
   - `ollama`: UP/DOWN
   - `hhapi`: UP/DOWN (проверяет токен)
   - `telegram`: UP/DOWN/UNKNOWN

2. **Логи приложения**:
   ```powershell
   Get-Content logs/hh-assistant.log -Tail 50
   ```

3. **Проверка расписания**:
   В логах должно быть:
   ```
   Starting scheduled vacancy check
   Fetched X new vacancies from HH.ru
   ```

---

### Вариант 2: Запуск в dry-run режиме (без реальных действий)

Этот вариант позволяет проверить работу приложения без реальных запросов к HH.ru.

#### Шаг 1-5: Выполните как в Варианте 1

#### Шаг 6: Настройка dry-run режима

1. Откройте `application.yml`
2. Найдите строку:
   ```yaml
   dry-run: false
   ```
3. Измените на:
   ```yaml
   dry-run: true
   ```

Или установите переменную окружения:
```powershell
$env:APP_DRY_RUN="true"
```

#### Шаг 7: Запуск

```powershell
./gradlew bootRun
```

В логах будет:
```
Dry-run mode enabled, skipping vacancy check
```

Приложение запустится, но не будет выполнять проверку вакансий.

---

### Вариант 3: Запуск без HH.ru (только проверка компонентов)

Можно запустить приложение без HH.ru токена для проверки:
- Подключения к БД
- Работы Ollama
- Health checks

1. **Не указывайте** `HH_ACCESS_TOKEN` в `.env`
2. Установите `dry-run: true` в `application.yml`
3. Запустите приложение

Health check для HH.ru будет показывать DOWN, но остальные компоненты будут работать.

---

## 🔍 Проверка работоспособности

### 1. Проверка Health Checks

Откройте в браузере:
```
http://localhost:8080/actuator/health
```

Ожидаемый результат:
```json
{
  "status": "UP",
  "components": {
    "db": { "status": "UP" },
    "ollama": { "status": "UP", "details": {...} },
    "hhapi": { "status": "UP", "details": {...} },
    "telegram": { "status": "UP" или "UNKNOWN" }
  }
}
```

### 2. Проверка логов

```powershell
# Просмотр последних 50 строк
Get-Content logs/hh-assistant.log -Tail 50

# Поиск ошибок
Select-String -Path logs/hh-assistant.log -Pattern "ERROR"
```

### 3. Проверка БД

```powershell
docker exec -it hh-postgres psql -U hh_user -d hh_assistant

# Проверка таблиц
\dt

# Проверка конфигураций
SELECT * FROM search_configs;

# Проверка вакансий
SELECT id, name, status FROM vacancies LIMIT 10;
```

### 4. Проверка Ollama

```powershell
# Проверка доступности
curl http://localhost:11434/api/tags

# Тестовый запрос
ollama run qwen2.5:7b "Привет! Как дела?"
```

---

## ⚙️ Настройка расписания

По умолчанию приложение проверяет вакансии каждые 15 минут.

Чтобы изменить интервал, отредактируйте `application.yml`:

```yaml
app:
  schedule:
    vacancy-check: "0 */15 * * * *"  # Каждые 15 минут
```

Формат cron: `секунды минуты часы день месяц день_недели`

Примеры:
- `"0 */5 * * * *"` - каждые 5 минут
- `"0 0 * * * *"` - каждый час
- `"0 0 9,18 * * *"` - в 9:00 и 18:00

---

## 🐛 Решение проблем

### Проблема: Docker не запускается

**Решение:**
1. Убедитесь, что WSL2 установлен и обновлен
2. В Docker Desktop: Settings → General → Use WSL 2 based engine
3. Перезапустите Docker Desktop

### Проблема: PostgreSQL не запускается

**Решение:**
```powershell
# Проверьте, не занят ли порт 5432
netstat -ano | findstr :5432

# Остановите и удалите контейнер
docker-compose down
docker volume rm hh-helper_postgres_data

# Запустите заново
docker-compose up -d postgres
```

### Проблема: Ollama не отвечает

**Решение:**
1. Проверьте, запущен ли сервис Ollama:
   ```powershell
   Get-Service | Where-Object {$_.Name -like "*ollama*"}
   ```

2. Перезапустите Ollama:
   ```powershell
   # Остановите
   Stop-Service Ollama
   # Запустите
   Start-Service Ollama
   ```

3. Проверьте порт:
   ```powershell
   netstat -ano | findstr :11434
   ```

### Проблема: Ошибка "No active search configurations"

**Решение:**
Создайте конфигурацию поиска в БД (см. Шаг 7 Варианта 1).

### Проблема: Health check для HH.ru показывает DOWN

**Решение:**
1. Проверьте токен в `.env`
2. Проверьте, что токен не истек
3. Попробуйте обновить токен на https://hh.ru/account/applications

### Проблема: Приложение не компилируется

**Решение:**
```powershell
# Очистите кеш
./gradlew clean

# Пересоберите
./gradlew build

# Проверьте версию Java
java -version  # Должна быть 17+
```

---

## 📊 Мониторинг

### Просмотр логов в реальном времени

```powershell
Get-Content logs/hh-assistant.log -Wait -Tail 20
```

### Проверка метрик (если добавлены)

```
http://localhost:8080/actuator/metrics
```

### Проверка информации о приложении

```
http://localhost:8080/actuator/info
```

---

## 🛑 Остановка приложения

1. **Остановка приложения**: `Ctrl+C` в терминале

2. **Остановка PostgreSQL**:
   ```powershell
   docker-compose stop postgres
   ```

3. **Полная остановка и удаление**:
   ```powershell
   docker-compose down
   ```

4. **Удаление данных БД** (осторожно!):
   ```powershell
   docker-compose down -v
   ```

---

## 📝 Следующие шаги

После успешного запуска:

1. **Настройте конфигурации поиска** под ваши требования
2. **Поместите ваше резюме** в папку `resumes/`
3. **Настройте Telegram** для получения уведомлений
4. **Мониторьте логи** для отслеживания работы
5. **Настройте расписание** под ваши нужды

---

## 💡 Полезные команды

```powershell
# Перезапуск приложения
./gradlew bootRun

# Просмотр логов БД
docker logs hh-postgres

# Подключение к БД
docker exec -it hh-postgres psql -U hh_user -d hh_assistant

# Проверка статуса Docker контейнеров
docker ps

# Очистка логов
Clear-Content logs/hh-assistant.log
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `logs/hh-assistant.log`
2. Проверьте health checks: `http://localhost:8080/actuator/health`
3. Убедитесь, что все сервисы запущены (PostgreSQL, Ollama)
4. Проверьте переменные окружения в `.env`

