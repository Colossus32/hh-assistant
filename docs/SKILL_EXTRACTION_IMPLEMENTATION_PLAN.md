# План реализации извлечения навыков из вакансий с помощью LLM

## Цель
Извлекать навыки из описаний вакансий HH.ru с помощью LLM, собирать статистику их встречаемости и рассчитывать процент популярности каждого навыка.

## Архитектура решения

### 1. Новые компоненты

#### 1.1. Сервис извлечения навыков
**Файл:** `src/main/kotlin/com/hhassistant/service/SkillExtractionService.kt`

**Ответственность:**
- Извлечение навыков из описания вакансии с помощью LLM
- Нормализация названий навыков (удаление дубликатов, синонимов)
- Валидация извлеченных навыков

**Методы:**
- `extractSkills(vacancy: Vacancy): List<String>` - основной метод извлечения
- `normalizeSkillName(skill: String): String` - нормализация названия
- `validateSkills(skills: List<String>): List<String>` - валидация и фильтрация

#### 1.2. Сущность для хранения навыков
**Файл:** `src/main/kotlin/com/hhassistant/domain/entity/Skill.kt`

**Структура:**
```kotlin
@Entity
@Table(name = "skills")
data class Skill(
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    val id: Long? = null,
    
    @Column(name = "name", unique = true, nullable = false)
    val name: String,
    
    @Column(name = "normalized_name", unique = true, nullable = false)
    val normalizedName: String,
    
    @Column(name = "occurrence_count", nullable = false)
    val occurrenceCount: Int = 0,
    
    @Column(name = "last_seen_at")
    val lastSeenAt: LocalDateTime? = null,
    
    @Column(name = "created_at", nullable = false)
    val createdAt: LocalDateTime = LocalDateTime.now(),
)
```

#### 1.3. Связь вакансий и навыков
**Файл:** `src/main/kotlin/com/hhassistant/domain/entity/VacancySkill.kt`

**Структура:**
```kotlin
@Entity
@Table(name = "vacancy_skills")
data class VacancySkill(
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    val id: Long? = null,
    
    @Column(name = "vacancy_id", nullable = false)
    val vacancyId: String,
    
    @Column(name = "skill_id", nullable = false)
    val skillId: Long,
    
    @Column(name = "extracted_at", nullable = false)
    val extractedAt: LocalDateTime = LocalDateTime.now(),
)
```

#### 1.4. Репозиторий для навыков
**Файл:** `src/main/kotlin/com/hhassistant/repository/SkillRepository.kt`

**Методы:**
- `findByName(name: String): Optional<Skill>`
- `findByNormalizedName(normalizedName: String): Optional<Skill>`
- `findTopSkills(limit: Int): List<Skill>` - топ навыков по популярности
- `findAllByOccurrenceCountGreaterThan(minCount: Int): List<Skill>`

#### 1.5. Сервис статистики навыков
**Файл:** `src/main/kotlin/com/hhassistant/service/SkillStatisticsService.kt`

**Ответственность:**
- Расчет статистики популярности навыков
- Агрегация данных о встречаемости
- Предоставление API для получения статистики

**Методы:**
- `updateSkillStatistics(vacancyId: String, skills: List<String>)` - обновление статистики
- `getTopSkills(limit: Int): List<SkillStatistics>` - топ навыков
- `getSkillFrequency(skillName: String): Double?` - процент встречаемости
- `recalculateAllStatistics()` - пересчет всей статистики

**DTO для статистики:**
```kotlin
data class SkillStatistics(
    val skillName: String,
    val occurrenceCount: Int,
    val totalVacanciesAnalyzed: Int,
    val frequencyPercentage: Double,
    val lastSeenAt: LocalDateTime?,
)
```

### 2. Интеграция в существующий пайплайн

#### 2.1. Модификация VacancyAnalysisService
**Изменения в:** `src/main/kotlin/com/hhassistant/service/VacancyAnalysisService.kt`

**Добавить:**
- Инжект `SkillExtractionService` и `SkillStatisticsService`
- После анализа вакансии вызывать извлечение навыков
- Сохранять навыки в БД и обновлять статистику

**Логика:**
```kotlin
// После успешного анализа вакансии
val extractedSkills = skillExtractionService.extractSkills(vacancy)
skillStatisticsService.updateSkillStatistics(vacancy.id, extractedSkills)
```

#### 2.2. Промпт для извлечения навыков
**Файл:** `src/main/resources/prompts/skill-extraction-prompt.txt`

**Содержание:**
```
Извлеки из описания вакансии список технических навыков, технологий, инструментов и требований.

Верни результат в формате JSON:
{
  "skills": ["Kotlin", "Spring Boot", "PostgreSQL", "Docker", "Kubernetes"]
}

Требования:
- Извлекай только конкретные технологии и навыки
- Игнорируй общие фразы типа "опыт работы", "коммуникабельность"
- Приводи названия к стандартному виду (например, "Kotlin" вместо "Kotlin Developer")
- Если навык упоминается несколько раз - включай только один раз
- Минимум 3 навыка, максимум 20
```

#### 2.3. Конфигурация промпта
**Изменения в:** `src/main/kotlin/com/hhassistant/config/PromptConfig.kt`

**Добавить:**
- `skillExtractionSystem: String` - системный промпт
- `skillExtractionTemplate: String` - шаблон промпта

### 3. API для получения статистики

#### 3.1. Контроллер статистики навыков
**Файл:** `src/main/kotlin/com/hhassistant/web/SkillStatisticsController.kt`

**Endpoints:**
- `GET /api/skills/top?limit=20` - топ навыков по популярности
- `GET /api/skills/{skillName}` - статистика конкретного навыка
- `GET /api/skills/stats` - общая статистика (сколько всего навыков, вакансий проанализировано)

#### 3.2. Telegram команда для получения статистики
**Изменения в:** `src/main/kotlin/com/hhassistant/service/TelegramCommandHandler.kt`

**Добавить команду:**
- `/skills` или `/top-skills` - получить топ навыков по популярности
- `/skills {limit}` - получить топ N навыков (например, `/skills 10`)

**Логика:**
- Вызывает REST API endpoint `/api/skills/top?limit={limit}`
- Форматирует ответ в читабельный вид для Telegram
- Показывает: название навыка, процент встречаемости, количество вакансий

**Пример ответа в Telegram:**
```
📊 <b>Топ навыков по популярности:</b>

1. Kotlin - 45.5% (150 вакансий)
2. Spring Boot - 36.4% (120 вакансий)
3. PostgreSQL - 28.8% (95 вакансий)
4. Docker - 25.0% (82 вакансии)
5. Kubernetes - 18.2% (60 вакансий)

Всего проанализировано: 330 вакансий
```

**Response пример:**
```json
{
  "skills": [
    {
      "name": "Kotlin",
      "occurrenceCount": 150,
      "frequencyPercentage": 45.5,
      "lastSeenAt": "2024-01-31T10:00:00"
    },
    {
      "name": "Spring Boot",
      "occurrenceCount": 120,
      "frequencyPercentage": 36.4,
      "lastSeenAt": "2024-01-31T09:30:00"
    }
  ],
  "totalVacanciesAnalyzed": 330
}
```

### 4. Миграция базы данных

#### 4.1. Создание таблиц
**Файл:** `src/main/resources/db/changelog/changes/YYYYMMDD-HHMMSS-add-skills-tables.xml`

**Таблицы:**
1. `skills` - основная таблица навыков
2. `vacancy_skills` - связь вакансий и навыков (many-to-many)

**Индексы:**
- `skills.normalized_name` - уникальный индекс
- `vacancy_skills.vacancy_id` - индекс для быстрого поиска
- `vacancy_skills.skill_id` - индекс для агрегации

### 5. Нормализация навыков

#### 5.1. Словарь синонимов
**Файл:** `src/main/resources/skills-synonyms.json`

**Пример:**
```json
{
  "Kotlin": ["Kotlin Developer", "Kotlin программист", "Kotlin разработчик"],
  "Spring Boot": ["SpringBoot", "Spring Framework", "Spring"],
  "PostgreSQL": ["Postgres", "PostgreSQL Database", "Postgres DB"]
}
```

#### 5.2. Сервис нормализации
**Логика:**
- Приведение к lowercase
- Удаление лишних пробелов
- Поиск в словаре синонимов
- Если не найдено - возврат оригинального названия

### 6. Оптимизация производительности

#### 6.1. Кэширование
- Кэшировать результаты извлечения навыков для одинаковых описаний
- Использовать `@Cacheable` на методах получения статистики

#### 6.2. Асинхронная обработка
- Извлечение навыков можно делать асинхронно после анализа
- Не блокировать основной пайплайн анализа

#### 6.3. Batch обновление статистики
- Вместо обновления при каждой вакансии - накапливать и обновлять батчами
- Или использовать scheduled task для пересчета статистики

### 7. Обработка ошибок

#### 7.1. Fallback стратегия
- Если LLM не смог извлечь навыки - использовать пустой список
- Логировать ошибки, но не прерывать основной пайплайн

#### 7.2. Валидация результатов
- Проверять, что извлечено минимум 3 навыка (или максимум 20)
- Фильтровать слишком общие слова ("опыт", "знание", "умение")

### 8. Метрики и мониторинг

#### 8.1. Метрики
- `skills.extracted.total` - всего навыков извлечено
- `skills.extraction.duration` - время извлечения
- `skills.extraction.errors` - ошибки извлечения
- `skills.statistics.recalculated` - пересчеты статистики

#### 8.2. Логирование
- Логировать количество извлеченных навыков для каждой вакансии
- Логировать топ-10 навыков при пересчете статистики

## Этапы реализации

### Этап 1: Базовая инфраструктура (1-2 часа)
1. ✅ Создать сущности `Skill` и `VacancySkill`
2. ✅ Создать репозитории
3. ✅ Создать миграцию БД
4. ✅ Протестировать структуру данных

### Этап 2: Извлечение навыков (2-3 часа)
1. ✅ Создать `SkillExtractionService`
2. ✅ Добавить промпт для извлечения навыков
3. ✅ Интегрировать с `OllamaClient`
4. ✅ Реализовать нормализацию навыков
5. ✅ Добавить валидацию результатов

### Этап 3: Статистика (1-2 часа)
1. ✅ Создать `SkillStatisticsService`
2. ✅ Реализовать обновление статистики
3. ✅ Реализовать расчет процентов встречаемости
4. ✅ Добавить методы получения топ навыков

### Этап 4: Интеграция (1 час)
1. ✅ Интегрировать в `VacancyAnalysisService`
2. ✅ Добавить извлечение навыков после анализа
3. ✅ Обновление статистики при сохранении анализа

### Этап 5: API (1 час)
1. ✅ Создать `SkillStatisticsController`
2. ✅ Добавить endpoints для получения статистики
3. ✅ Добавить документацию API

### Этап 6: Оптимизация (1-2 часа)
1. ✅ Добавить кэширование
2. ✅ Оптимизировать запросы к БД
3. ✅ Добавить индексы

### Этап 7: Тестирование (1-2 часа)
1. ✅ Unit тесты для `SkillExtractionService`
2. ✅ Unit тесты для `SkillStatisticsService`
3. ✅ Integration тесты для API
4. ✅ Тесты нормализации навыков

### Этап 8: Документация (30 минут)
1. ✅ Обновить README
2. ✅ Добавить примеры использования API
3. ✅ Документировать формат промпта

## Оценка времени
**Общее время:** ~10-15 часов работы

## Риски и митигация

### Риск 1: LLM извлекает неточные навыки
**Митигация:**
- Добавить валидацию и фильтрацию
- Использовать словарь валидных навыков
- Периодически проверять качество извлечения

### Риск 2: Производительность (медленное извлечение)
**Митигация:**
- Делать извлечение асинхронно
- Кэшировать результаты
- Использовать batch обработку

### Риск 3: Дублирование навыков (синонимы)
**Митигация:**
- Словарь синонимов для нормализации
- LLM может помочь с нормализацией
- Периодическая очистка дубликатов

## Дальнейшие улучшения

1. **Категоризация навыков** - группировка по типам (языки, фреймворки, БД)
2. **Тренды во времени** - отслеживание роста/падения популярности
3. **Корреляции** - какие навыки часто встречаются вместе
4. **Сравнение с резюме** - какие навыки есть в вакансиях, но нет в резюме
5. **Рекомендации** - какие навыки стоит изучить на основе популярности

## Зависимости

- Существующий `OllamaClient` - для вызова LLM
- Существующий `VacancyAnalysisService` - для интеграции
- Существующая БД (PostgreSQL) - для хранения данных
- Существующий `PromptConfig` - для конфигурации промптов

## Файлы для создания/изменения

### Новые файлы:
1. `src/main/kotlin/com/hhassistant/service/SkillExtractionService.kt`
2. `src/main/kotlin/com/hhassistant/service/SkillStatisticsService.kt`
3. `src/main/kotlin/com/hhassistant/domain/entity/Skill.kt`
4. `src/main/kotlin/com/hhassistant/domain/entity/VacancySkill.kt`
5. `src/main/kotlin/com/hhassistant/repository/SkillRepository.kt`
6. `src/main/kotlin/com/hhassistant/web/SkillStatisticsController.kt`
7. `src/main/resources/prompts/skill-extraction-prompt.txt`
8. `src/main/resources/skills-synonyms.json`
9. `src/main/resources/db/changelog/changes/YYYYMMDD-HHMMSS-add-skills-tables.xml`

### Изменяемые файлы:
1. `src/main/kotlin/com/hhassistant/service/VacancyAnalysisService.kt` - интеграция
2. `src/main/kotlin/com/hhassistant/service/TelegramCommandHandler.kt` - добавление команды /skills
3. `src/main/kotlin/com/hhassistant/config/PromptConfig.kt` - промпты
4. `src/main/kotlin/com/hhassistant/metrics/MetricsService.kt` - метрики

### Тесты:
1. `src/test/kotlin/com/hhassistant/service/SkillExtractionServiceTest.kt`
2. `src/test/kotlin/com/hhassistant/service/SkillStatisticsServiceTest.kt`
3. `src/test/kotlin/com/hhassistant/web/SkillStatisticsControllerTest.kt`
4. `src/test/kotlin/com/hhassistant/repository/SkillRepositoryTest.kt`
5. `src/test/kotlin/com/hhassistant/service/TelegramCommandHandlerTest.kt` - обновить для команды /skills

