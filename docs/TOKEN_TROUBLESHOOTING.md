# Решение проблем с токеном HH.ru API

## Проблема: 403 Forbidden даже после получения токена

Если вы получаете 403 Forbidden после получения токена через OAuth, проверьте следующее:

### 1. Проверьте настройки приложения на dev.hh.ru

1. Откройте https://dev.hh.ru
2. Войдите в свой аккаунт
3. Найдите ваше приложение (по Client ID)
4. Проверьте настройки:
   - **Redirect URI** должен точно совпадать: `http://localhost:8080/oauth/callback`
   - **Права доступа**: Убедитесь, что приложение имеет права на доступ к API вакансий
   - **Статус приложения**: Должно быть активно

### 2. Проверьте логи приложения

После перезапуска приложения проверьте логи:

```
✅ [WebClient] HH.ru WebClient configured with Authorization header
   Token length: 64 chars
   Token prefix: USERJUSSF1...
   Auth prefix: Bearer
```

Если видите:
```
❌ [WebClient] HH.ru WebClient configured WITHOUT Authorization header
```
Значит токен не загружен из .env.

### 3. Проверьте, что токен отправляется в запросах

В логах должно быть:
```
🔑 [WebClient] Request to https://api.hh.ru/vacancies?...: Authorization header present (71 chars, prefix: Bearer USERJUSSF1...)
```

Если видите:
```
❌ [WebClient] Request to ...: NO Authorization header!
```
Значит токен не отправляется в запросах.

### 4. Проверьте формат токена

Токен должен:
- Начинаться с `USER` (для пользовательских токенов)
- Иметь длину 64 символа
- Быть сохранен в `.env` как `HH_ACCESS_TOKEN=ваш_токен` (без кавычек и пробелов)

### 5. Проверьте права приложения

Если токен отправляется, но все равно 403, возможно приложение не имеет нужных прав:

1. Откройте https://dev.hh.ru
2. Найдите ваше приложение
3. Проверьте раздел "Права доступа" или "Scopes"
4. Убедитесь, что включены права на:
   - Чтение вакансий
   - Доступ к публичному API

### 6. Получите новый токен с правильными правами

1. Откройте: `http://localhost:8080/oauth/authorize`
2. **Важно**: При авторизации на HH.ru убедитесь, что вы даете все необходимые разрешения
3. После авторизации токен автоматически сохранится
4. **Обязательно перезапустите приложение**

### 7. Проверьте через тестовый endpoint

```
http://localhost:8080/api/token/test?keywords=Java
```

Этот endpoint покажет:
- Загружен ли токен
- Работает ли токен
- Детальную информацию об ошибке

## Частые проблемы

### Проблема: Токен работает в браузере, но не в приложении

**Причина**: В браузере используется сессия/cookies, а приложение использует OAuth токен. Это разные механизмы авторизации.

**Решение**: Убедитесь, что OAuth токен получен правильно и имеет нужные права.

### Проблема: Токен сохранен, но не работает после перезапуска

**Причина**: Возможно, токен не загружается из .env или загружается неправильно.

**Решение**:
1. Проверьте формат `.env` файла (без кавычек, без пробелов вокруг `=`)
2. Проверьте кодировку файла (должна быть UTF-8)
3. Убедитесь, что файл находится в корне проекта

### Проблема: 403 Forbidden даже с валидным токеном

**Причина**: Приложение не имеет нужных прав на dev.hh.ru.

**Решение**: Проверьте настройки приложения на https://dev.hh.ru и убедитесь, что включены все необходимые права доступа.

## Диагностика

Используйте следующие endpoints для диагностики:

1. **Информация о токене**: `http://localhost:8080/api/token/info`
2. **Тест токена**: `http://localhost:8080/api/token/test?keywords=Java`
3. **Health check**: `http://localhost:8080/actuator/health`

## Контакты

Если проблема не решается:
1. Проверьте документацию HH.ru API: https://dev.hh.ru
2. Проверьте логи приложения на наличие ошибок
3. Убедитесь, что приложение на dev.hh.ru настроено правильно



